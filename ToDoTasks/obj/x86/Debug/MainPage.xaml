<Page x:ConnectionId='1'
    x:Class="ToDoTasks.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToDoTasks"

    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <ListView x:ConnectionId='5' x:Name="TasksList"                                                                                IsItemClickEnabled="True">
                <ListView.ItemTemplate>
                    <DataTemplate                                 >
                        <Grid x:ConnectionId='7'>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="250" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
                                <TextBlock x:ConnectionId='9'                    FontSize="15" Margin="0,0,1,0"/>
                                <TextBlock x:ConnectionId='10'                      FontSize="15" Margin="0,0,1,0"/>                                
                            </StackPanel>
                            <Grid>
                                <Button x:Name="Button_RemoveTask" Content="Remove" Background="Red"
                                        HorizontalAlignment="Right" VerticalAlignment="Top"
                                        Height="36" Canvas.ZIndex="1"
                                        Width="36"  Command="{Binding DataContext.DeleteCommand, ElementName=TasksList}" CommandParameter="{Binding ID}"/>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Row="1" Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <TextBlock Text="Selected task:" FontWeight="Bold" Margin="10"/>
                <TextBlock x:ConnectionId='2' x:Name="TextBlock_FName" Margin="5" />
                <TextBlock x:ConnectionId='3' x:Name="TextBlock_LName" Margin="5"/>
                <TextBlock x:ConnectionId='4' x:Name="TextBlock_Description" Margin="5"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

